# Backend listen address
#
# Local dev (only accessible from the same machine):
#   MQLM_ADDR=127.0.0.1:8088
# Server/LAN (accessible from other devices on the network):
#   MQLM_ADDR=0.0.0.0:8088
MQLM_ADDR=0.0.0.0:8088

# SQLite path (relative to backend/ is fine)
MQLM_DB_PATH=./data/mql_manager.db

# Admin token for Authorization: Bearer <token>
# IMPORTANT:
# - Required when binding to 0.0.0.0 / non-localhost.
# - Used by admin UI and /api/* endpoints.
# - /public/* endpoints for Android remain accessible.
# Generate a strong token, for example:
#   openssl rand -hex 32
MQLM_ADMIN_TOKEN=change-me

# CORS (comma-separated origins)
MQLM_CORS_ORIGINS=http://127.0.0.1:3000,http://localhost:3000
